import React from 'react';
import { Main, ScrollVertical, Column, Row, Title } from '@/ui';

export default function HomeScreen({ navigation, }) {
    return (
        <Main >
            <ScrollVertical>
                <Title align="center">Home</Title>
            </ScrollVertical>
        </Main>
    )
}
