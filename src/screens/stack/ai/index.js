import React, { useState, useEffect } from 'react';
import { Main, ScrollVertical, Column, Label, Title, Row, Button, } from '@/ui';
export default function Screen({ navigation, }) {
    return (
        <Main>
            <ScrollVertical>
                <Column>
                    <Title>AI</Title>
                </Column>
            </ScrollVertical>
        </Main>
    )
}